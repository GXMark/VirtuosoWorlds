// Code generated by ObjectBox; DO NOT EDIT.

#include "vw.obx.hpp"

const obx::Property<Actor, OBXPropertyType_Long> Actor_::id(1);
const obx::Property<Actor, OBXPropertyType_String> Actor_::key(2);
const obx::Property<Actor, OBXPropertyType_String> Actor_::value(3);

void Actor::_OBX_MetaInfo::toFlatBuffer(flatbuffers::FlatBufferBuilder& fbb, const Actor& object) {
    fbb.Clear();
    auto offsetkey = fbb.CreateString(object.key);
    auto offsetvalue = fbb.CreateString(object.value);
    flatbuffers::uoffset_t fbStart = fbb.StartTable();
    fbb.AddElement(4, object.id);
    fbb.AddOffset(6, offsetkey);
    fbb.AddOffset(8, offsetvalue);
    flatbuffers::Offset<flatbuffers::Table> offset;
    offset.o = fbb.EndTable(fbStart);
    fbb.Finish(offset);
}

Actor Actor::_OBX_MetaInfo::fromFlatBuffer(const void* data, size_t size) {
    Actor object;
    fromFlatBuffer(data, size, object);
    return object;
}

std::unique_ptr<Actor> Actor::_OBX_MetaInfo::newFromFlatBuffer(const void* data, size_t size) {
    auto object = std::make_unique<Actor>();
    fromFlatBuffer(data, size, *object);
    return object;
}

void Actor::_OBX_MetaInfo::fromFlatBuffer(const void* data, size_t, Actor& outObject) {
    const auto* table = flatbuffers::GetRoot<flatbuffers::Table>(data);
    assert(table);
    outObject.id = table->GetField<obx_id>(4, 0);
    {
        auto* ptr = table->GetPointer<const flatbuffers::String*>(6);
        if (ptr) {
            outObject.key.assign(ptr->c_str(), ptr->size());
        } else {
            outObject.key.clear();
        }
    }
    {
        auto* ptr = table->GetPointer<const flatbuffers::String*>(8);
        if (ptr) {
            outObject.value.assign(ptr->c_str(), ptr->size());
        } else {
            outObject.value.clear();
        }
    }
}

const obx::Property<Collision, OBXPropertyType_Long> Collision_::id(1);
const obx::Property<Collision, OBXPropertyType_String> Collision_::key(2);
const obx::Property<Collision, OBXPropertyType_String> Collision_::value(3);

void Collision::_OBX_MetaInfo::toFlatBuffer(flatbuffers::FlatBufferBuilder& fbb, const Collision& object) {
    fbb.Clear();
    auto offsetkey = fbb.CreateString(object.key);
    auto offsetvalue = fbb.CreateString(object.value);
    flatbuffers::uoffset_t fbStart = fbb.StartTable();
    fbb.AddElement(4, object.id);
    fbb.AddOffset(6, offsetkey);
    fbb.AddOffset(8, offsetvalue);
    flatbuffers::Offset<flatbuffers::Table> offset;
    offset.o = fbb.EndTable(fbStart);
    fbb.Finish(offset);
}

Collision Collision::_OBX_MetaInfo::fromFlatBuffer(const void* data, size_t size) {
    Collision object;
    fromFlatBuffer(data, size, object);
    return object;
}

std::unique_ptr<Collision> Collision::_OBX_MetaInfo::newFromFlatBuffer(const void* data, size_t size) {
    auto object = std::make_unique<Collision>();
    fromFlatBuffer(data, size, *object);
    return object;
}

void Collision::_OBX_MetaInfo::fromFlatBuffer(const void* data, size_t, Collision& outObject) {
    const auto* table = flatbuffers::GetRoot<flatbuffers::Table>(data);
    assert(table);
    outObject.id = table->GetField<obx_id>(4, 0);
    {
        auto* ptr = table->GetPointer<const flatbuffers::String*>(6);
        if (ptr) {
            outObject.key.assign(ptr->c_str(), ptr->size());
        } else {
            outObject.key.clear();
        }
    }
    {
        auto* ptr = table->GetPointer<const flatbuffers::String*>(8);
        if (ptr) {
            outObject.value.assign(ptr->c_str(), ptr->size());
        } else {
            outObject.value.clear();
        }
    }
}

const obx::Property<Material, OBXPropertyType_Long> Material_::id(1);
const obx::Property<Material, OBXPropertyType_String> Material_::key(2);
const obx::Property<Material, OBXPropertyType_String> Material_::value(3);

void Material::_OBX_MetaInfo::toFlatBuffer(flatbuffers::FlatBufferBuilder& fbb, const Material& object) {
    fbb.Clear();
    auto offsetkey = fbb.CreateString(object.key);
    auto offsetvalue = fbb.CreateString(object.value);
    flatbuffers::uoffset_t fbStart = fbb.StartTable();
    fbb.AddElement(4, object.id);
    fbb.AddOffset(6, offsetkey);
    fbb.AddOffset(8, offsetvalue);
    flatbuffers::Offset<flatbuffers::Table> offset;
    offset.o = fbb.EndTable(fbStart);
    fbb.Finish(offset);
}

Material Material::_OBX_MetaInfo::fromFlatBuffer(const void* data, size_t size) {
    Material object;
    fromFlatBuffer(data, size, object);
    return object;
}

std::unique_ptr<Material> Material::_OBX_MetaInfo::newFromFlatBuffer(const void* data, size_t size) {
    auto object = std::make_unique<Material>();
    fromFlatBuffer(data, size, *object);
    return object;
}

void Material::_OBX_MetaInfo::fromFlatBuffer(const void* data, size_t, Material& outObject) {
    const auto* table = flatbuffers::GetRoot<flatbuffers::Table>(data);
    assert(table);
    outObject.id = table->GetField<obx_id>(4, 0);
    {
        auto* ptr = table->GetPointer<const flatbuffers::String*>(6);
        if (ptr) {
            outObject.key.assign(ptr->c_str(), ptr->size());
        } else {
            outObject.key.clear();
        }
    }
    {
        auto* ptr = table->GetPointer<const flatbuffers::String*>(8);
        if (ptr) {
            outObject.value.assign(ptr->c_str(), ptr->size());
        } else {
            outObject.value.clear();
        }
    }
}

